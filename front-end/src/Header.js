/**import React from 'react';
// pode-se passar objeto de proriedade: props ou pode-se passar a propriedade especifica
// que eu quero trabalhar acompnhada de {} ex:{children}
function Header(props){
    return (
        <header>
           <h1>{props.children}</h1>
        </header>
            //<h1>{props.children}</h1>
    
            //<h1>{props.title}</h1>

    );
}

export default Header;
*/